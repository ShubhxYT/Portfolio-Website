# Vite 6 Cloudflare Deployment

Update the project's Vite development dependency from version 5 to version 6.
Regenerate `package-lock.json` so Cloudflare installs the supported version.

No application code or Vite build configuration changes are required. Verify the
production build before committing and pushing the dependency update.
